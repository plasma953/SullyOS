# APK 真机冒烟清单（M1 Task 5 扩展版 · 2026-09-10）

每次出包装机后逐项打勾。发现坏项 → 记录截图 + 设备/Android 版本 → 按 `plans/apk-audit-fixes-2026-09-10.md` 的「待真机项」归类后再立项修复（不盲修）。

## 基础（M1 原 Task 5）

- [ ] 桌面/图标正常渲染
- [ ] 设置 → 主代理中转：地址 `https://43451695.xyz` + 客户端令牌 → 「测试连接」显示 ✅
- [ ] 新建/导入角色 → 聊天发一条 → 收到回复（走中转）
- [ ] 杀进程重开：角色与聊天记录仍在（IndexedDB 持久）
- [ ] 设置页可开可改，退出重进保留
- [ ] 主动消息区显示降级提示而非红屏（设计内）

## 推送现状（审计后预期）

- [ ] 启动无崩溃；logcat 里应有一条 `[UnifiedPush] runtime init failed` 的 warn（完全无此条也正常——说明模块没加载）
- [ ] 主动消息：到点后打开 App 能补收到（outbox 补收）
- [ ] 设置 → 主动消息 2.0：workerUrl 已预填 `https://43451695.xyz/amsg`，徽章探活绿

## 网络/中转链路

- [ ] 设置 → 主代理中转测试：断网/错 token 时显示明确错误文案，**且不会自动改写中转地址**（B1 修复后）
- [ ] 聊天长时间生成（>2 分钟）不被掐（网梯子环境另算，见 notes/ethernet-branch-context.md「NAT 空闲超时」节）
- [ ] MCP 链路（若配了 MCP 服务器）：工具调用成功
- [ ] amsg 即时对话：发送后「正在输入」→ 收到回复

## UI 完整性（零安全区命中名单逐屏过）

- [ ] 外卖、购物、预设、笔友会（小说）、终端 五个 App：底部手势条不遮关键按钮
- [ ] 聊天输入框：键盘弹起不被遮挡（Keyboard resize:body）
- [ ] 长聊天消息、图片、弹窗层级正常
- [ ] 深浅色切换、横竖屏切换不破版
- [ ] 平板/大屏（若测）：desktopMode auto 切桌面形态，手机框仍可用

## OTA

- [ ] 设置里「检查更新」不报错（清单已置空 = no-op），或显示「未配置清单」类文案
