# ethernet 分支 · 项目背景与约定

> 这份文件是二改工作的长期上下文。新会话开工前先读一遍；改完记得保持内容最新（2026-09-06 记录）。

## 仓库关系

- **原版（上游）**：https://github.com/qegj567-cloud/SullyOS —— 本仓库 `origin/master` 跟踪它，分叉点 `b0f5fd24`（上游 PR #615，Android v3.4.9 时期）。
- **二改仓**：origin = plasma953/SullyOS，**`ethernet` 分支是唯一主力开发线，今后所有修改都只做在 ethernet 上**（不做别的分支，不直接改 master）。
- `ethernet` 领先上游约 78 个提交（+24811 行 / 246 文件），落后约 43 个。落后部分多为 amsg2/记忆/协同小修，不少已被同名 cherry-pick 等价移植；真正未合入的主要有：上游 `AGENTS.md` 更名、analytics 快照合并上报、amsg2 worker 2.6.0-next.26/27 的修复、上游 MCP 产品化版（协议协商）。
- 同步上游的惯例是**按提交 cherry-pick 等价改动**，而不是整分支 merge；接上游新改动前先确认 ethernet 里是否已有同名等价提交。

## ethernet 分支的侧重点

按分量排序，主题是「云端依赖搬回自己手里 + 拟真生活系统大扩张」：

1. **VPS 后端自托管（核心）**：新增 `vps-backend/`（Node 原生宿主 + Cloudflare Workers 兼容垫片，D1→SQLite，bin/config/deploy/src）。amsg2 主动消息纯 VPS 化、Instant Push UI 整体退役、主代理 MCP 循环代理、三通道备份。「ethernet」得名于此——自组网自托管。
2. **拟真商业系统**：外卖/购物双 App（OSM 真实店面 × OFF 真实商品 670 条、淘宝式界面、全 SVG 商品图）；银行拟真化（21 家银行 SVG + 品牌渐变卡面 + char 卡视图 + 代记收入 INCOME）；char 视角下单链路（ORDER 主动点单）；LLM 拟真购买记录。
3. **自习室深度改造**：EPUB 阅读器（原文/AI 总结双模式、配色主题、重复图片去重、浮动导师）、AI 分层总结、讲课模式、多字体库。
4. **角色世界观增强**：透视窗（char 查看用户真实设备操作记录，Supabase+RLS）、角色级地理位置 × 天气联动 × 中国节假日状态机、联系人关系网发散、时光契约日历化与重复规则。
5. **提示词目录化 + 预设 App**：全系统提示词目录化、注入预览与三段式标签、预设管理。
6. **MCP 增强**：公网 CORS 修复、主代理中转、连接方式选择、调用次数自定义、跨轮结果记忆。
7. **UI/桌面层**：主网格 4x4、紧凑图标、loader dots、番茄钟（水球动画+同伴鼓励+惩罚）等。

## 新增功能时的硬约定

**UI、动效、美化风格必须与 ethernet 现有风格保持一致**：动手前先读 `docs/design-system.md` 对照表，再看 ethernet 里同类界面的既有写法（配色、圆角、动效节奏、CSS 组织方式），在既有模式上扩展，不照抄上游新风格、不引入全新视觉语言。各 App 的既有风格（如聊天/设置系的深色玻璃风）**只在其 App 内延续，不要互相串味**。

**唯一例外专区——番茄钟「铅笔手绘风」**（2026-09-06 整套落地，改番茄钟前必读本条）：

- 风格基因：纸底 `#f5f0e8` + 墨色 `#3a3630` 文字；`border-2 dashed` 虚线框；手绘不规则圆角（如 `255px 15px 225px 15px / 15px 225px 15px 255px`）；阴影用 45° 交叉线伪元素（不用纯色投影）；楷体栈 `'Kaiti SC','STKaiti','KaiTi',serif`；**禁**渐变、纯白底、玻璃模糊（backdrop-blur）；动效短促（120-160ms 微抖入场、按下沉 1px）。
- 原子件只从 `apps/pomodoro/SketchKit.tsx` 取（SketchBox / SketchButton / SketchLabel / SketchHatch / SketchKeyframes / accentFill）；颜色只来自 `utils/pomodoroPrefs.ts` 的 `accent` / `waterColor`（用户可自定义，8+6 预设）。改番茄钟任何界面必须复用这套原子件，不许散写新样式。
- 边界：手绘风**只属于番茄钟**，不要扩散到其他 App；其他 App 的组件也不要把手绘风元素带进去。番茄钟内部五个子模块（SketchKit / WaterBallSketch / CompanionBall / PomodoroSettings / UsageHeatmap）风格必须保持同一基因，接线约束见 `utils/pomodoroSketch.wiring.test.ts`。

## VPS 部署布局（已确认可连，2026-09-06）

**改码工作流（硬约定）：所有内容一律在本机修改 → push 到 `origin/ethernet` → VPS 上 pull + 重启生效。不在 VPS 上直接改仓库里的文件。** 只有只存在于 VPS 的运行时文件（`/opt/sullyos/.env`、systemd unit、Caddyfile、data/logs）需要改动时才在 VPS 上动手。

SSH：`root@<your-vps-ip>:<ssh-port>`（opencode 的 vps 工具里叫 `default`）。Ubuntu 24.04，2G 内存，Node v22 + pnpm 11。

- `/opt/sullyos/sullyos-repo`：plasma953/SullyOS 的 clone，checkout 在 `ethernet`，跟着 `origin/ethernet` 走。**注意它只到 origin/ethernet——本地领先未 push 的提交不会自动生效，先 push 再上 VPS 更新。**
- `/opt/sullyos/vps-backend`：部署目录（bin/config/deploy/src + node_modules），`systemd` 服务名 `sullyos.service`，入口 `bin/run-all.js`，日志 `/opt/sullyos/logs/sullyos.log`，环境变量 `/opt/sullyos/.env`。
- 端口全部只听 127.0.0.1，由 Caddy 反代出公网：**8830 main-agent**、**8831 instant-push**、**8832 amsg（共享密钥鉴权）**、8833 proactive-push、8834 wake-bridge、8835 heartbeat（唯一事实来源：`vps-backend/config/services.js`）；域名 `your-vps.example.com` / `mcp.your-vps.example.com` / `oc.your-vps.example.com`（真实域名只放服务器 .env 与部署面板，不进仓库）。
- `sullyos-dufs.service`：WebDAV(dufs) 备份存储（127.0.0.1，端口与凭据在 .env）。
- 机器上还有 `phone-chat-gateway.service`（/root/apps/vps-chat-gateway）及 kaleidoscope / ruota / xhs-mcp / theseus-brain 等 MCP 服务，main-agent 已把后三个 MCP 挂进工具池。
- 观察项：main-agent `/health` 显示 `llmConfigured:false, providers:0`——若按设计是凭据随请求走则正常，改主代理 LLM 配置时留意。

## 仓库现状与坑（2026-09-06 更新）

- ✅ 番茄钟手绘风改造已落地（本地 `39878aee`，已 push；纯前端，VPS 未动）：全 App 铅笔手绘风；主页设背景图/整体配色/水球单色/消息形态；可拖动角色悬浮球（轻点循环文字/语音/混合，语音自动播放）；12 周热力图（历史上限 100→500）；停止改一层轻确认后立即结束，惩罚后台生成自动落聊天+记忆，惩罚弹窗已删。
- ✅ UTF-8 乱码大修复（本地 `b19fffb9`）：8 文件 203 处 U+FFFD 截断从 git 历史逐行恢复，activeMsgRuntime 两处 `= 启动` 裸标识符（cherry-pick 出生即坏，曾致 49 个测试失败）补为 `'上线补收'`，新增 `utils/mojibakeGuard.test.ts` 全仓护栏（扫到 U+FFFD 即测试红）。
- ⚠️ 乱码专项（项目事实）：本仓库历史上多次被 Windows AI 会话截断 UTF-8，肇事的特性提交波次 f732464a / c75ffa57 / ec8c983e / 7a68eb2b / dcb78439 / 939e6525。日常编码纪律与乱码修复套路见全局 AGENTS.md「文件编码」节；已加 `utils/mojibakeGuard.test.ts` 全仓护栏（扫到 U+FFFD 即测试红），动过含中文文件后跑它。
- 🧷 **AI 防乱码操作清单（2026-09-06 实测结论）**：乱码分两层——①显示层：PowerShell 5.1 输出管线把 UTF-8 中文解码成问号状替换符（U+FFFD）（git 里的字节是干净的，`git log` 经 python 验过 `utf8-ok=True`），只污染眼睛；②文件层：真正的 U+FFFD 进源码，来自 shell 写文件或引用了被显示层污染的文本。遵守：写文件只用 Write/Edit 工具，绝不用 shell 重定向/`Set-Content`/`echo` 写文件；含中文的 oldString 只从 Read 工具输出逐字取，bash 输出里的中文只看行号、不复制文字；bash 命令参数避免中文（搜中文用 Grep 工具）；commit message 用英文；绝不在 Write/Edit 参数里放 U+FFFD 字符本身做示意（护栏认字不认意图），用“U+FFFD”文字描述代替；每次动过含中文文件后跑 `pnpm vitest run utils/mojibakeGuard.test.ts` + 字节扫 FFFD（诊断只打印 ASCII）。
- ✅ 上游 MCP 产品化合并已收尾（本地 `53dc9664`，已 push、已同步 VPS）：`collectMcpFireServers` 双方融合（ethernet relay 物化 + 上游 destructive 过滤/完整工具载荷），Settings 标题保留 ethernet StatusBadge，`APP_VERSION` 维持 `v3.8 (Slimdown)` 不随上游。上游 8df8e594 的等价合入到此完成。
- ✅ 全手机地点真实化已落地（2026-09-06，spec 在 `docs/superpowers/specs/2026-09-06-location-system-design.md`）：高德 Web 服务 Key 进实时感知（`RealtimeConfig.amapApiKey`，个人认证免费；地理编码 15 万次/月、POI 搜索 5000 次/月，地点库按城市缓存 30 天）；`worker/index.js` 新增 `/amap` 透传端点（key 由 query 透传、代理不存）。人物（神经链接地点卡验证+`已验证`标）/ 日程（生成注入真实地点清单+`locationMeta` 对齐）/ 日历（修好死的头部天气+用户城市并排）/ 世界（可选 `WorldProfile.city`，不设=纯架空不受扰）/ 见面（OBSERVE 地点对齐+HUD 地址子行）/ 搬家（MOVE_TO 地理编码验证，换城丢旧坐标）/ 用户档案（所在城市卡，GPS 只记省市）。实时世界块新增「用户那边」段（用户城市+天气，`userPerceptionEnabled` 默认开；用户城市经 `sully_user_city_v1` 镜像上云，amsg worker 双城天气快照按城分键）。外卖/购物不动。**未部署事项**：`/amap` 需代理实例重新部署 `worker/index.js` 才生效（VPS 上 pull+重启；公共默认实例等作者更新）；高德无海外权限，海外城市回落 Open-Meteo。
- ✅ Spark 混入豆瓣小组真实内容已落地（2026-09-06，App 未改名）：`worker/index.js` 新增 `/social/douban/topics|topic|comments` + `/social/img`（rexxar 匿名接口，iPhone UA + m 站 Referer，列表 120s 内存缓存）；新建 `utils/doubanSource.ts`（归一化/小组管理，localStorage 键 `spark_douban_groups`，删帖记 `spark_douban_hidden`）；`SocialApp` 刷新先拉豆瓣再 LLM（热点标题喂给既有 prompt 蹭热度，LLM 调用零新增，符合计费红线）、详情页懒加载正文+真实评论、卡片真实图分支；预置杭州/深圳/武汉/租房/美食/情感/电影/旅行 8 个当日实测活跃组（北京组是 2022 冻结快照、shanghai 等 slug 404，已避开）。验证：新测试 29 个（doubanSource 17 + doubanProxy 11 + merge 1）+ 锚/存储套件全过，本次文件 tsc 零命中，U+FFFD 干净，真机链路冒烟（列表/详情/评论）通过。**未部署事项**：公共默认 worker 实例需重新部署 `worker/index.js` 豆瓣段才生效，未部署前自动降级纯 LLM 流。
- ✅ B站/XHS 链接内容提取已落地（2026-09-06，本地未提交）：聊天发 B站链接/b23.tv/裸BV号 → `worker/index.js` 新增 `/bilibili/view|player|storyboard|asset`（稿件元数据/Player-CC字幕/videoshot预览帧/资源代理，白名单+Referer/UA过风控；AI小结要登录已删）→ 新建 `utils/bilibiliCard.ts`（检测/BV号/字幕文本化/雪碧图切帧，复用 webpage_card 零新类型）；字幕/简介全量喂角色，帧随主请求 image_url 或拼图识图（每卡终身1次，见💰条）；XHS desc全量+评论按赞排序。验证：TDD（新测试 worker12+bilibili19+webpageCard6+vision2+xhs3）+ 真B站接口校验（纠正videoshot端点名/单帧尺寸/conclusion登录墙）+ 相关234项绿 + tsc触碰文件零命中 + U+FFFD干净 + worker真实链路冒烟通过。**未部署事项**：`/bilibili/*` 需重新部署 worker 才生效，未部署前自动降级 apizero 卡。
- 本机开发环境：pnpm 不在 PATH，用 `corepack pnpm@9.15.9`（lockfile v9）；项目 `.npmrc` 指向腾讯镜像但经常 502，装依赖加 `--registry=https://registry.npmjs.org/`。git 身份仅本仓库配置为 **plasma953 / plasma953@users.noreply.github.com**（与 `18fd25f8` 一致）。
- ⚠️ 身份教训：Tosd0（ID 65720409）是真实存在的 GitHub 账号，是上游部分提交的**作者**，但 ethernet 的**提交者**必须是 plasma953。挑身份只认 `committer` 里 plasma953 的那条，不许从最近几条 log 照抄（2026-09-06 顶上 4 个 Tosd0 署名提交保持不动，不再改写）。
- tsc 全量有 48 个**存量**错误（MemoryPalaceApp/CompanionHome/activeMsgRuntime 等，含历史中文乱码），与 MCP 无关；判据：本次触碰的文件在 tsc 输出里零命中。
- 工作区几百个「已修改」文件是 **CRLF 噪音**（`core.autocrlf=true`），不是真实改动；commit 时只加真正动过的文件。
- 📦 **提交节奏（2026-09-06）**：同一项目里可能多条线同时改不同地方，改完一块不要立刻 commit。攒到任务/阶段收尾再统一提交，提交前 `git diff --stat` 逐 hunk 确认归属（2026-09-06 曾把工作区 24 行高德 WIP 误收进 desktop 提交，后拆回）。
- 💰 **LLM 计费口径（2026-09-06，B站/XHS 链接提取立项确认）**：本项目 LLM 按调用次数计费，新功能的设计红线是「零额外 LLM 调用」：抓取/排序/切片全部走本地或 HTTP 计算，不调 LLM 做总结；识图只能走「多帧拼 1 图 + 单次调用 + metadata 永久缓存」模式（复用 `materializeVisionDescriptions` 既有机制），多模态主模型走 `image_url` 随主请求发送，纯文本模型无识图配置时压平成占位——绝不为单个功能新起计费调用。
- 🏗️ **功能构建流程（2026-09-06 地点系统实测沉淀，最小可复用链路）**：spec 先行（`docs/superpowers/specs/<日期>-<主题>-design.md`）→ 按「纯函数叶子 → 缓存层 → UI → prompt 注入 → worker 联动」顺序实施 → 每块收尾跑对应单测 → 全量门禁：`corepack pnpm@9.15.9 vitest run`（本次 407 文件 / 4922 用例）+ `pnpm build:workers`（amsg bundle 变了就一起提交）+ `mojibakeGuard`（动中文文件必跑）+ `tsc --noEmit`（触碰文件零命中）→ 英文 message 按功能块提交 → push `origin/ethernet`。本次新增单测：amapCore 17 / geoMatch 7 / cityPlaces 14 / amapProxy 5，扩展既有 realtimeWorldCore / datePrompts / worldHome / amsgToolPack / worker realtimeWorld 5 处。教训：`vi.useFakeTimers()` 的文件里禁真 IndexedDB 读（用 localStorage 同步镜像代替）；`RealtimeConfig` 有 types.ts 与 realtimeContext.ts 两份声明，加字段要两边同步。
- ✅ 蓝牙外设已落地（2026-09-06，spec 见 `docs/superpowers/specs/2026-09-06-bluetooth-design.md`）：设置页「蓝牙」卡片 + Modal（配对 BLE 外设、连接管理、GATT 控制台：枚举/读/写 hex-text/订阅 notify/保存为一键指令；设备详情标题栏可改名，角色看到的就是改后的名字）；数据存 IndexedDB asset（`ble_registry_v1`）进备份。角色侧：已连设备+指令清单进 volatile prompt（零设备零 token）；前台聊天内置工具 `ble_send_command`（仅发已保存具名指令，60 秒指纹防打转且只记成功）；实时感知宫格蓝牙开关（关掉后 prompt 与工具同时沉默）。版本号 `v3.9 (Wireless Touch)`，文档 `docs/bluetooth.md`。验证：新测试 38（engine 18 / registry 10 / bridge 10，含指纹回归用例）+ 全量 400 文件 / 4836 用例绿 + tsc 触碰文件零命中 + mojibakeGuard 绿；终审 2 Important（开关失信、失败写指纹）+ 3 Minor 全修。*待实测*：真机链路未经浏览器验证（需桌面 Chrome + 真实 BLE 设备走配对→枚举→写值→订阅→保存指令→角色触发）；iOS Safari / 安卓 WebView 不支持（灰态）。
- 🧭 **CORS 系统规则（2026-09-07 起，2026-09-11 契约统一后更新；新增浏览器→第三方请求前必读）**：SullyOS 是纯浏览器静态应用（页面源 vercel.app），一切第三方 API 默认浏览器直发、受同源策略约束——主流 API（LLM 网关如 opencode.ai、地图等）基本都不回 CORS 头，直连必 `Failed to fetch`。这不是回归，是架构属性：每接一个新服务都会重撞一次（此前已撞出主代理中转/中心 worker/mcp-proxy/opencode-proxy 四层）。规则：① 新调用点先问「这条谁在发」，浏览器发就必须有 CORS 或走既有通道；② 通道选型——LLM 系（含拉 `/models` 列表）一律走主代理中转 `/agent/v1/*`（`/v1/models` 透传已落地，供应商 key 走 `X-Relay-Target-Authorization` 头，不进 URL/日志），搜索/抓取/备份走中心 worker（`getProxyWorkerUrl()`），MCP 走 relay 或 mcp-proxy，终端走 opencode-proxy；**前端统一走 `utils/externalRequest.ts` 的 `externalFetch(url, { route })` 声明语义（llm/worker/mcp/direct），路由交给入口、失败日志自动带通道标注**；③ 全局拦截器只改写 `/chat/completions`，其余路径不会自动中转——新端点用统一入口；④ 排查先用 SYSTEM ERROR 日志：Resource Timing 有记录 + no-cors 复检可达 = 响应无 CORS 头（网络没问题），与真断网一秒区分。前端拉模型列表唯一入口 `utils/modelList.ts:fetchChatModelList`（已收口到统一入口），Settings 两处（主 API/识图）+ 协作 + 三个活动页（WhiteDay/Valentine/520）已全部收口。**服务端 2026-09-11 起统一为契约（设计/计划：`docs/superpowers/specs/2026-09-11-cors-unification-design.md` / `docs/superpowers/plans/2026-09-11-cors-unification-plan.md`）：全部通道预检「净化回显请求头/方法 + 204 + 鉴权前」，新自定义头零配置放行；参考实现 `worker/shared/cors.ts`，行为由 `worker/corsContract.test.ts` 对每个通道跑同一组断言锁死（改通道后跑它）；单文件 worker（index.js / mcp-proxy / opencode-proxy / main-agent / heartbeat / wake-bridge）内联同一逻辑，保持「整份复制即可部署」。仍待迁移到统一入口的存量点：embeddings（记忆宫殿）、realtime HN 直连、TTS 系列、webpageExtractor（行为不受影响，仅诊断标注）。**
- ✅ opencode.ai（Zen/Go）接入（2026-09-07）：LLM 上游切 OpenCode Go（Base URL `https://opencode.ai/zen/go/v1`，模型 `deepseek-v4-flash`，VPS `.env` 兜底 `LLM_*` + 前端预设直传同一套——预设优先、env 只兜底）；main-agent 调 opencode 上游自动带 `User-Agent: SullyOS-MainAgent/1.0` + `x-opencode-session`（进程级稳定 UUID，Go 防滥用要求）；sully-proxy Worker 已用 CF API 更新为 ethernet 版（含 `/amap` `/bilibili` `/social` `/latent` + 预检回显请求头）。*待用户*：手机端 API 预设换同一套后自发一句即时对话触发凭据重登记（实现期零真实 LLM 调用，由用户发起验证）。
- 📡 **长生成被掐（2026-09-07 实测定性）**：手机端聊天 28~116s 零字节死亡、VPS 日志 `ERR_STREAM_PREMATURE_CLOSE`——定性为手机↔VPS 段「无流量连接」被运营商 NAT/梯子空闲超时掐死（VPS 侧 pipeline 报 premature close = 手机先掉线，生成在服务端继续、已计费但送不回）。两段修法：①用户开预设「流式输出」（透明流式升级把 40+ 处旁路 stream:false 全改流式）；②main-agent SSE 加 **15s `: ping` 心跳**（makeSse.ping，SSE 标准注释行，SseAssembler 天然忽略），静默段字节持续流动。残余风险：连接建立本身失败（72s 无响应头类，心跳救不了）与 opencode 上游偶发 premature close——前者频发时下一杠杆是给 `43451695.xyz` 开 CF 橙云代理（需核 SSL 模式 Full，灰云直连 US 裸 IP 本质不稳）；后者按计费红线不做自动重发。
- ✅ 推送测试按钮（2026-09-07）：设置页「推送订阅状态」面板新增「发送测试通知」——后端 `POST /push-test`（配置门之后：鉴权口径与 selfUpdate 一致，读库内已登记订阅，VAPID 直发 `messageKind:'test'` 真推送；30s 同窗拦截回 429；零 LLM、零写库）→ SW `case 'test'` 只转交页面（`active-msg-test` 事件，不写 inbox）→ 面板按 testId 认领报收。横幅由包层按 notification 展示（前后台都弹）。验证：worker 路由 6 条 + client 2 条 mock 单测；真机端到端由用户亲手点按钮验证。注意：SW 侧未知 kind 会落 content 写聊天气泡，新增 kind 必须配独立 case。
- ✅ opencode 身份头全覆盖 worker 侧 LLM 调用（2026-09-08，commit `042059ae`，spec 见 `docs/superpowers/specs/2026-09-08-opencode-session-header-design.md`）：修掉即时对话「Error from provider: Request is missing x-opencode-session」——amsg / instant-push 两 worker 入口装全局 fetch 补丁（`utils/llmIdentity.ts`：opencode.ai 域的 POST /chat/completions 自动补 `User-Agent` + `x-opencode-session`，isolate 级稳定 UUID；其余请求原样放行），覆盖 callLlm / 情绪评估 / 后台 fireKinds 全部直连调用。AMSG_BUNDLE_VERSION 与 INSTANT_WORKER_VERSION 双双 2026-09-08；两台 CF worker 已用用户提供的 API token 现场重部署并验过（`/config-check` ok、`/version` 2026-09-08、secrets 全保留——经验：脚本上传 metadata 里**不提** secret 即保留，带空 text 会被 10021 拒）。🚢 **测试通道事实（用户确认）**：用户手机端跑的是远端 vercel 构建（源 origin/ethernet）——任何要用户实测的改动**必须 push** 才能测到，worker bundle 也要随提交进仓库；CF worker 本体可走 CF API 直更，但前端/站点只认 push。
- 本仓库仍是 `CLAUDE.md` + docs 体系（上游已改叫 `AGENTS.md`，ethernet 未跟随）；项目事实以本文与 CLAUDE.md 文档地图为准。
- ✅ 桌面小手机四项修正（2026-09-08，计划 `docs/superpowers/plans/2026-09-08-desktop-shell-refine.md`）：外框 393:852 宽度优先 + dock 图标按框宽 clamp 缩放；滚轮翻页桥 `utils/wheelPager.ts` 接 6 处（纵向可滚容器优先原生）；删 DesktopFrame 灵动岛；PiP 小窗只渲染带框手机（408×884，无模糊背景）。门禁：全量 427 文件 / 5025 用例（storageOptimize 4 项并发抖动、单跑 84 全过）+ tsc 触碰零命中 + mojibake 绿。
- ✅ 启动器 Dock 互换 + 电脑主页一屏 + 全机动效（2026-09-11，三提交 `d21060ea` / `b84e2b3e` / `13292b03`；spec `docs/superpowers/specs/2026-09-11-launcher-dock-motion-design.md`、计划 `plans/2026-09-11-launcher-dock-motion-plan.md`）：
  - **Dock 互换**：Dock 恒 4 格；新增 `context/LauncherLayoutContext.tsx`（手机与电脑左侧 Dock/桌面网格共用唯一顺序源）+ `hooks/useLauncherDrag.ts`（长按 520ms 编辑，跨容器拖动靠 pointer capture + elementFromPoint）；`utils/launcherLayout.ts` 纯函数（normalize/reorder/swapDockAndGrid），网格与 Dock 成员互斥、写回 `theme.launcher*Order`；`DesktopDock` 删 QUICK_EXTRAS 改读共享顺序；电脑端非主页不可编辑。
  - **桌面一屏**：`apps/Launcher.tsx` 桌面分支改 `h-full overflow-hidden` 两列（左列 `clamp(280px,22vw,340px)` + compact 时钟/角色卡/日历，极端小窗列内滚动；右列 `components/desktop/DesktopAppGrid.tsx` 用 `utils/fitGrid.ts` 按容器自适应列数/单元，全部 App 一屏，装不下才内部滚动）；桌面网格改用共享顺序（修掉此前忽略用户排序）。
  - **动效补全**：17 处硬切换（动作面板改 translateX track；表情/聊天壳/查手机选人/角色列表分页改 keyed 方向滑入；外观/日程/自习室/音乐/群聊/查手机/手账 tab 改 keyed 纯淡入）+ 4 项微交互（首屏 stagger、编辑态 jiggle、弹层退场、按压档位审计）。中央 token `index.html`：`page-in-l/r`、`fade-soft`、`fade-out-soft`（均带 reduced-motion 降级）；新增 `hooks/useExitPresence.ts`，Modal/ConfirmDialog/ErrorDialog 接退场；`PerCharAvatarPicker` 私有动画并入中央 token。版本 `v3.13 (Dock & Motion)`。
  - 门禁：全量 425 文件 / 5031 用例（storageOptimize 4 项与 networkFailureDiagnosis 为已知全量并发抖动，各自单跑 84/46 全过）+ tsc 触碰零命中 + mojibake 绿 + 新增单测 19 条。*待用户实测*：三端互换同步、桌面 5 档视口一屏、各处新动效手感与 reduced-motion；未 push，Vercel 测试通道需 push 后可见。
- ✅ 全 App 视图/覆盖层动效覆盖（2026-09-11 续，阶段 D，计划 `plans/2026-09-11-app-view-motion-coverage.md`）：4 份盘点覆盖 ~40 App；整屏视图导航统一「前进 `page-in-l` / 返回目标 `page-in-r`（MemoryPalace/Room/PixelHome 带 navDir 方向状态 + `key={view}` 强制重放）/ 平级 `fade-soft`」，覆盖 MemoryPalace、Room/PixelHome、WorldHome、VRWorld、ThemeMaker、LifeSim、PersonaSim/DreamTheater、CheckPhone（SubAppShell 统一入场）、DateApp/DateSession/StoryTheater、CallApp、Terminal、Browser、Journal、Novel（NovelWriter 加 className prop）、Songwriting、GameApp、Guidebook、Handbook（组件根承载）、FAQ、Takeout、Shopping、XhsStock、Tarot、Bank、Social、VoiceDesigner、XhsFreeRoam、Worldbook 等；另补 40+ 组件型覆盖层入场（遮罩 fade-in + 面板 slide-up/pop-in）。修掉 MemoryPalace 3 处内联 `fade-in` 关键帧名错误（动画从未生效）。提交 `b6ff708`/`a25ef68`/`87f1d746`/`6995dc7f` + 文档提交。门禁：全量 425/5031 全绿 + tsc 触碰零命中 + FFFD 0。*未做*：D6 覆盖层退场（`useExitPresence` 批量接入），页面级「退出」已由返回目标入场覆盖。
- ✅ 桌面占位组件缩小 + 全 App 按钮动效（2026-09-11，计划 `plans/2026-09-11-desktop-polish-button-motion.md`）：
  - **按钮动效**：`index.html` 中央兜底 `button, button *` 状态过渡（颜色/描边/阴影 200ms、transform/scale 150ms；自带 `transition-*` 的按钮类选择器优先，保留原样）+ 无 `active:` 档位按钮统一 `scale:0.98` 轻压（`scale` 属性不冲定位 transform，`inset-0` 整屏遮罩跳过）+ reduced-motion 降级；另把 49 个「有过渡但只过渡 transform、选中变色仍瞬跳」的按钮（ChatInputArea 22、ScheduleApp 5、GameApp 4 等 18 文件）升级 `transition-all duration-200`。
  - **桌面缩小**：Gallery 相册人物/照片网格、Study 书架（导入教材卡）、Shopping 商品网格改 `useLayoutMode` 桌面分支 `repeat(auto-fill,minmax(132-176px,1fr))`；Journal 选本网格桌面限宽 860 居中 + 空白角色页 `max-w-sm max-h-[420px] m-auto` 紧凑占位；手机端类名零变化。空态 `col-span-2` → `col-span-full`。
  - 门禁：全量 425 文件 / 5031 用例全绿 + tsc 触碰零命中（存量 45）+ mojibake 绿 + FFFD/BOM 0 + `vite build` 通过。提交 `a522ec49`，已 push origin/ethernet（Vercel 测试通道可用）。*待用户实测*：桌面 1440×900 各网格/占位尺寸、外卖分类按钮等选中过渡手感、reduced-motion。
- ✅ 半屏手机形态滚轮翻页修复（2026-09-11，计划 `plans/2026-09-11-wheel-pager-scroller-detect.md`）：窗口缩小进入手机形态（无框/手机框）后主页滚轮只能单方向翻页——根因是 `utils/wheelPager.ts` 的 `findVerticalScroller` 只看 `scrollHeight - clientHeight`，把「内容溢出但被 `contain: paint` 裁剪、实际滚不动」的页面容器当成纵向滚动容器，把向下滚轮让给了一个无法滚动的元素。修法：只有 computed `overflow-y` ∈ auto/scroll/overlay 才算真滚动容器（尺寸判断放前面，计算样式只对通过者调用）；`apps/Launcher.tsx:910` 翻页容器显式 `overflow-y-hidden`；`utils/wheelPager.test.ts` +3 条 jsdom 用例（不可滚溢出祖先不拦截 / 真容器未到底不翻页、到底翻页 / 未到顶不翻页、到顶翻上一页）共 8 条。门禁：全量 429 文件 / 5091 用例全绿 + tsc 触碰零命中 + mojibake 绿 + FFFD 0。提交见仓库记录，已 push（供 Vercel 实测）。
